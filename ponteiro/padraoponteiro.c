#include <stdio.h>
#include <stdlib.h>
 char *func(char str[], char str2[]){
 	int n=2, i, contador, x=6;
 	char *p;
 	p = (char *) malloc (sizeof(char)* x);
	for(i=0;i<8;i++){
	
	p = str;
}
 return(p);

 }
int main(){

char str[10]="A aula";
char str2[10]={"outra"};
char *p;
p = func(str, str2);


	printf("%s", p);


free(p);
}
